version: 2

models:
  - name: example_model
    description: "A simple dbt model that returns a sample record"
    columns:
      - name: run_date
        description: "The date the model was run"
      - name: message
        description: "A sample message column"
      - name: example_value
        description: "An example numeric column"
        tests:
          - not_null
